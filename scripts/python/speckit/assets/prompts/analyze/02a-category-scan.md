---
stage: file_analysis_phase1
requires: 01c-script-execution
outputs: category_patterns
version: 3.4.0
next: 02b-deep-dive.md
time_allocation: 25%
---

# Stage 2A: Category Scan (Phase 1)

## Purpose

Scan 15-20% of files in EACH category to identify patterns. This phase provides a broad overview before deep-diving into priority areas.

**Time Allocation:** 25% of file analysis effort

---

## State Management

The CLI provides all context via template variables. **Do not read state.json directly.**

**Available template variables:**

- `{project_path}` - Project being analyzed
- `{analysis_dir}` - Analysis folder path (root)
- `{data_dir}` - Data folder for JSON files (`{analysis_dir}/data/`)
- `{reports_dir}` - Reports folder for MD files (`{analysis_dir}/reports/`)
- `{scope}` - Analysis scope (A or B)
- `{context}` - Additional context
- `{concern_type}`, `{current_impl}`, `{target_impl}` - Scope B specific

**CLI Utility Commands:**

- `speckitadv write-data <filename> --content '<json>'` - Write JSON to data/ folder
- `speckitadv file-stats <filepath>` - Get file statistics (lines, size)

---

## Pre-Check: Verify Previous Stage

Required files from Stage 1C:

- `{data_dir}/file-manifest.json` - File listing
- `{data_dir}/tech-stack.json` - Detected technologies

**IF files missing:** STOP - Return to Stage 1C

---

## Step 1: Load File Manifest

Read the file manifest generated by the script:

```bash
cat {data_dir}/file-manifest.json
```

This contains:
- All files in the project
- File sizes
- Last modified dates
- File types

---

## Step 2: Categorize Files

Group files from the manifest into these categories:

| Category | Patterns to Match |
|----------|-------------------|
| **Controllers/Routes** | `*Controller*`, `*Route*`, `*Handler*`, `*Endpoint*`, `routes/*`, `controllers/*` |
| **Services/Business** | `*Service*`, `*Manager*`, `*Provider*`, `services/*`, `business/*` |
| **Models/Entities** | `*Model*`, `*Entity*`, `*Domain*`, `models/*`, `entities/*`, `domain/*` |
| **Repositories/DAOs** | `*Repository*`, `*Dao*`, `*Store*`, `repositories/*`, `data/*` |
| **Configurations** | `*.config.*`, `*.properties`, `*.yml`, `*.yaml`, `*.env*`, `config/*` |
| **Security/Auth** | `*Security*`, `*Auth*`, `*Permission*`, `*Role*`, `security/*`, `auth/*` |
| **Middleware** | `*Middleware*`, `*Filter*`, `*Interceptor*`, `middleware/*` |
| **Utilities/Helpers** | `*Util*`, `*Helper*`, `*Common*`, `utils/*`, `helpers/*`, `common/*` |
| **Tests** | `*Test*`, `*Spec*`, `*.test.*`, `*.spec.*`, `test/*`, `tests/*`, `__tests__/*` |

---

## Step 3: Category Scan Execution

For EACH category, scan 15-20% of files (minimum 3 files per category if available).

### 3.1 Controllers/Routes Scan

---
⏸️ **[STOP: SCAN_CONTROLLERS]**

Scan files in Controllers/Routes category:

1. Select 15-20% of controller files (min 3)
2. For each file, extract:
   - HTTP endpoints (method, path)
   - Request/response DTOs referenced
   - Auth requirements (`@PreAuthorize`, `@Secured`, middleware decorators)
   - Error handling patterns
   - Validation rules

**Progress Output:**

```text
[Phase 1 - Category Scan]
Scanning Controllers/Routes...
✓ {file1}: Found {n} endpoints, Auth: {type}
✓ {file2}: Found {n} endpoints, Auth: {type}
✓ {file3}: Found {n} endpoints, Auth: {type}
...
✓ Controllers: {scanned}/{total} files scanned

```

---

### 3.2 Services/Business Logic Scan

---
⏸️ **[STOP: SCAN_SERVICES]**

Scan files in Services/Business Logic category:

1. Select 15-20% of service files (min 3)
2. For each file, extract:
   - Workflows and business rules
   - External integrations (APIs, message queues)
   - Data transformations
   - Transaction boundaries (`@Transactional`, commit/rollback)
   - Caching patterns

**Progress Output:**

```text
Scanning Services/Business Logic...
✓ {file1}: Business logic: {summary}
✓ {file2}: Integrations: {list}
✓ {file3}: Transactions: {pattern}
...
✓ Services: {scanned}/{total} files scanned

```

---

### 3.3 Models/Entities Scan

---
⏸️ **[STOP: SCAN_MODELS]**

Scan files in Models/Entities category:

1. Select 15-20% of model files (min 3)
2. For each file, extract:
   - Entity relationships (OneToMany, ManyToMany, etc.)
   - Validation constraints (`@NotNull`, `@Size`, etc.)
   - Computed properties
   - Database mappings (`@Table`, `@Column`)

**Progress Output:**

```text
Scanning Models/Entities...
✓ {file1}: Entity: {name}, Relationships: {count}
✓ {file2}: Entity: {name}, Validations: {count}
✓ {file3}: Entity: {name}, Mappings: {type}
...
✓ Models: {scanned}/{total} files scanned

```

---

### 3.4 Repositories/DAOs Scan

---
⏸️ **[STOP: SCAN_REPOSITORIES]**

Scan files in Repositories/DAOs category:

1. Select 15-20% of repository files (min 3)
2. For each file, extract:
   - CRUD patterns
   - Custom queries (JPA, SQL, NoSQL)
   - Query complexity
   - Caching strategies
   - Transaction handling

**Progress Output:**

```text
Scanning Repositories/DAOs...
✓ {file1}: Queries: {count}, Custom SQL: {yes/no}
✓ {file2}: Pattern: {CRUD/custom}
✓ {file3}: Caching: {strategy}
...
✓ Repositories: {scanned}/{total} files scanned

```

---

### 3.5 Remaining Categories Quick Scan

---
⏸️ **[STOP: SCAN_REMAINING]**

Quick scan of remaining categories (Security, Middleware, Utilities):

**Security/Auth:**
- Auth mechanism type (JWT, OAuth, SAML, Session)
- RBAC/ABAC patterns
- Password hashing

**Middleware:**
- Request/response processing
- Logging patterns
- Error handling

**Utilities:**
- Common patterns
- Date/time handling
- Validation functions

**Progress Output:**

```text
Scanning Security/Auth...
✓ Auth type: {type}, RBAC: {yes/no}

Scanning Middleware...
✓ Patterns: {list}

Scanning Utilities...
✓ Helpers found: {count}

```

---

## Step 4: Compile Category Patterns

Create a summary of patterns found in each category:

```json
{
  "category_scan": {
    "controllers": {
      "files_scanned": {count},
      "total_files": {count},
      "coverage": "{percentage}%",
      "patterns": {
        "api_style": "{REST/GraphQL/RPC}",
        "auth_types": ["{list}"],
        "validation": "{pattern}",
        "error_handling": "{pattern}"
      }
    },
    "services": {
      "files_scanned": {count},
      "total_files": {count},
      "coverage": "{percentage}%",
      "patterns": {
        "transaction_style": "{pattern}",
        "integrations": ["{list}"],
        "caching": "{present/absent}"
      }
    },
    "models": {
      "files_scanned": {count},
      "total_files": {count},
      "coverage": "{percentage}%",
      "patterns": {
        "orm_type": "{JPA/Hibernate/Sequelize/etc}",
        "relationship_styles": ["{list}"],
        "validation_approach": "{pattern}"
      }
    },
    "repositories": {
      "files_scanned": {count},
      "total_files": {count},
      "coverage": "{percentage}%",
      "patterns": {
        "query_style": "{ORM/native SQL/mixed}",
        "caching": "{strategy}",
        "complexity": "{low/medium/high}"
      }
    },
    "security": {
      "auth_mechanism": "{type}",
      "authorization": "{RBAC/ABAC/ACL}",
      "password_hashing": "{algorithm}"
    },
    "middleware": {
      "logging": "{framework}",
      "error_handling": "{pattern}"
    },
    "utilities": {
      "helper_count": {count},
      "common_patterns": ["{list}"]
    }
  }
}

```

---

## Step 5: Save Category Patterns

Write the category patterns JSON using CLI command:

```bash
speckitadv write-data category-patterns.json --content '<json>' --analysis-dir "{analysis_dir}"
```

This saves to `{data_dir}/category-patterns.json` and will be used by subsequent stages for deep-dive analysis.

---

## Output Summary

```text
═══════════════════════════════════════════════════════════
  SUBSTAGE COMPLETE: 02a-category-scan (Phase 1)

  Files Scanned: {count}/{total} ({percentage}%)
  Time Used: 25% allocation

  Pattern Summary:
    API Style: {REST/GraphQL/etc}
    Auth: {JWT/OAuth/Session/etc}
    ORM: {JPA/Hibernate/Sequelize/etc}
    Caching: {Redis/Memcached/None/etc}

  Proceeding to Phase 2: Deep Dive
═══════════════════════════════════════════════════════════

```

---

## Next Substage

Run: `speckitadv analyze-project`

The CLI will auto-detect the current stage and emit the next prompt (02b-deep-dive).
